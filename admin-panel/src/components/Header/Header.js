import React from 'react';

const Header = () => {
  return (
    <div style={styles.container}>
      sdsdf
    </div>
  );
};

const styles = {
  container: {
    width: '100%',
    display: 'flex',
    background: 'black',
    height: '64px',
    borderBottom: '1px solid #222'
  }
}

export default Header;