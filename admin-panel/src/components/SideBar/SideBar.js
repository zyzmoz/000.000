import React from 'react';

const SideBar = () => {
  return (
    <div style={styles.container}>
      asdas
    </div>
  );
};

const styles = {
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    maxWidth: '250px',
    background: '#000',
    height: '100%'
  }
}

export default SideBar;